export const DEFAULT_OPTIONS = {
  enableKeyReceiver: true,
  zoom: [20, 50, 100, 120, 200],
  images: [],
};

export const COMMAND_STATE_DISABLED = 0;
export const COMMAND_STATE_NORMAL = 1;
export const COMMAND_STATE_ACTIVE = 2;
